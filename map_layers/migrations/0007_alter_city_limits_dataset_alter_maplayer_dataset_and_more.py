# Generated by Django 5.0 on 2024-02-03 20:47
# Manual edit: Instead of deleting both the `Dataset` model and database table, only the model is deleted,
# and the database table is moved to the `datasets` app.
import django.db.models.deletion
from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('datasets', '0001_initial'),
        ('map_layers', '0006_city_citydatasetkeyvalue'),
    ]

    operations = [
        migrations.AlterField(
            model_name='city',
            name='limits_dataset',
            field=models.ForeignKey(help_text='Jeu de données utilisé pour les limites de la ville. Les limites de la ville seront générées à partir de ce jeu de données.Si le jeu de données est supprimé, les limites de la ville seront également supprimées.', on_delete=django.db.models.deletion.CASCADE, to='datasets.dataset', verbose_name='Jeu de données des limites'),
        ),
        migrations.AlterField(
            model_name='maplayer',
            name='dataset',
            field=models.ForeignKey(help_text='Jeu de données utilisé pour la couche. La couche sera générée à partir de ce jeu de données.Si le jeu de données est supprimé, la couche sera également supprimée.', on_delete=django.db.models.deletion.CASCADE, to='datasets.dataset', verbose_name='Jeu de données'),
        ),

        # Manual edit: Instead of deleting both the `Dataset` model and database table, only the model is deleted,
        # and the database table is moved to the `datasets` app.
        migrations.SeparateDatabaseAndState(
            state_operations=[
                migrations.DeleteModel(
                    name='Dataset',
                ),
            ],
            database_operations=[
                migrations.AlterModelTable(
                    name='dataset',
                    table='datasets_dataset',
                ),
            ]
        ),

    ]
